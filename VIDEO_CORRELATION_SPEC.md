# LogGuide — 映像信号検出＋クロスモーダル相関 実装指示

Claude Codeへの実装指示書（第2弾）。AI_ANALYSIS_SPEC.md の全項目は実装済み。
本指示では、音声オンリーだった解析に映像側のシグナルを加え、音声×映像の相関イベントを生成する。

## 前提（実装済みのもの）

- 録画基盤（画面＋音声）、ビューア（再生・シーク・マーカー表示・ジャンプ）
- 音声解析パイプライン：VADチャンク分割 → whisper.cpp文字起こし → 信号レベル検出（無発話・音量） → LLM classify → JSONL追記
- LLM設定システム（logguide.toml、役割別、空文字列で機能無効化）
- GPU三段縮退
- コーディング規約は cpp-coding-style スキルに従う。クラス命名は「対象 + 役割」パターン

## 背景と目的

LogGuideの強みは単一シグナルの精度ではなく、同期記録された複数シグナルの統合にある。
現状の音声解析は分類正答率5割程度で運用中（recall優先で妥協済み）。映像シグナルとの相関により、
無口なテスターへの対応と、単独シグナルでは確度の低いイベントの裏付けを実現する。

映像の「意味」解析（VLM等）は行わない。フレーム差分ベースの信号レベル検出のみ。
重い処理は入れない — 映像側はダウンスケール後の差分計算だけで完結させること。

## 実装項目

### 1. 映像信号検出（フレーム差分ベース)

録画中のキャプチャフレームを低解像度（例：160x90程度、要調整）にダウンスケールし、
一定間隔（例：2〜4fps相当のサンプリング、要調整）で前フレームとの差分量を計算する。

検出イベント：

| イベント | 条件 | JSONL type |
|---|---|---|
| 画面停滞 | 差分量が閾値以下の状態が一定時間継続（初期値30秒、設定可能） | `screen_static_start` / `screen_static_end` |
| 画面遷移頻発 | 差分スパイクが短時間に連続（メニュー開閉・リトライ連打の候補） | `screen_thrash` |
| 暗転・ロード | ほぼ単色（黒/白）フレームの継続 | `screen_blank_start` / `screen_blank_end` |

実装上の要求：

- 処理はCPUで完結させる（GPUはゲーム・whisper・LLMに譲る）
- ダウンスケールはキャプチャパイプラインの既存リソースを流用できるなら流用する
- 閾値・サンプリングレート・停滞判定時間はすべて logguide.toml で設定可能にする
- 差分アルゴリズムはシンプルに始める（ピクセル差分の総和 or 平均絶対差）。SSIM等の高級な手法は不要

```toml
# logguide.toml 追加セクション
[video_analysis]
enabled = true
sample_fps = 3
downscale_width = 160
static_threshold = 0.02      # 正規化差分量
static_duration_ms = 30000
blank_luma_threshold = 0.05
```

### 2. クロスモーダル相関エンジン

音声イベントと映像イベントを突き合わせ、単独より確度の高い合成イベントを生成する。

v1で実装する相関ルールは以下の3本のみ。ルールエンジン化や汎用DSLは作らない（YAGNI）：

| 合成イベント | 条件 | JSONL type |
|---|---|---|
| 詰まり候補 | 無発話区間 と 画面停滞 が重なる（重複期間が閾値以上） | `stuck_candidate` |
| 想定外反応 | confusion/surprise タグの発話の直前N秒以内（初期値5秒）に画面遷移がある | `unexpected_reaction` |
| 集中状態（除外用） | 無発話 だが 画面差分が活発 → 無発話単独イベントの重要度を下げる | `focus_likely` |

実装上の要求：

- 相関判定は録画中にリアルタイムで行う必要はない。数十秒遅れの遅延評価で十分
  （映像イベントと音声イベントの両方が確定してから突き合わせる）
- 合成イベントは元イベントへの参照を持つこと：

```jsonl
{"time_ms":195000,"type":"stuck_candidate","sources":[{"type":"silence_start","time_ms":191000},{"type":"screen_static_start","time_ms":193000}],"overlap_ms":45000}
```

- `focus_likely` は新規マーカーを増やすのではなく、該当する無発話イベントに `suppressed: true` を付与する形でもよい（ビューア側でのフィルタが楽な方を選ぶ）

### 3. ビューア側の対応

- 新イベントtype（映像系・合成系）のマーカー表示を追加。合成イベントは音声・映像と区別できる色/形にする
- マーカーのフィルタUI：type別に表示ON/OFFできるチェックボックス群（ImGui）
- `stuck_candidate` はデフォルトで最も目立つ表示にする（LogGuideの価値提案の中心となるイベントのため）
- `suppressed: true` のイベントはデフォルト非表示、フィルタで表示可能

### 4. サマリーへの反映

LLM summarize のプロンプトに合成イベントを含める。
「無発話が3回ありました」ではなく「詰まっていた可能性のある区間が3箇所あります（各タイムスタンプ）」と
報告できるようにする。

## 実装順序

1. 映像信号検出（フレーム差分・停滞・暗転） → JSONL出力まで
2. ビューアで映像イベントのマーカー表示（既存マーカー機構の拡張）
3. クロスモーダル相関（3ルール）
4. フィルタUI
5. summarize プロンプト更新

1→2 を先に通し、実際の録画で映像イベントの閾値（static_threshold等）を実測調整してから 3 に進むこと。
閾値が不適切なままだと相関ルールの評価ができない。

## テスト観点

- 映像解析ON時のCPU使用率増分の実測（録画・whisper同時稼働状態で）
- ジャンル差の確認：アクション（画面が常に動く）とノベル/パズル（画面が静的）で停滞検出が破綻しないか
  - 静的なゲームでは screen_static が常時発火する可能性が高い。その場合の抑制策（発火頻度の上限等）を検討し、結果を報告すること
- 暗転検出がゲーム内ムービー・意図的な暗転演出で誤発火する頻度
- 相関イベントの sources 参照が正しいイベントを指しているか
- video_analysis.enabled = false で従来どおり音声のみで動作すること（縮退原則の維持）

## 対象外（今回やらない）

- VLM等による映像の意味解析
- 入力ログ（キーボード/マウス/ゲームパッド）との相関 — Phase 2の入力ログ実装後
- 相関ルールの汎用エンジン化・外部定義化 — ルールが10本を超えそうになってから検討
- FaceCam
